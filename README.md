# homebrew-issuectl

Homebrew tap for [issuectl](https://github.com/jarimustonen/issuectl).

```sh
brew install jarimustonen/issuectl/issuectl
```

The `Formula/issuectl.rb` formula is automatically updated by the
upstream repo's release workflow on each version tag.
